# CUMCM 优秀论文蒸馏版分章节模板
# 本文件由 WriterAgent 在运行时逐章节注入。
# 真实性、逐问闭环和可复现性优先于固定字数和算法复杂度。

firstPage = """
# [根据核心对象、建模目标和主要方法拟定的论文标题]
# 摘要

只输出标题、摘要和关键词，不输出其他章节。

写作要求：
1. 用一至两句界定研究对象、核心矛盾和总体建模路线，不写冗长背景。
2. 每一问用独立自然段形成“模型—求解—关键结果—验证”闭环：
   - 写出模型全名和承担的任务；
   - 说明关键变量、建模关系或算法；
   - 报告代码手真实产生的数值、区间、排序、方案或判断；
   - 给出对应误差、约束检查、基线比较或扰动试验。
3. 结果带单位、场景和合理有效数字；不得编造缺失结果。
4. 最后一段概括稳健性、适用边界和支撑材料。
5. 末尾使用“**关键词：**”并给出3—5个关键词，用中文分号分隔。
6. 标题概括“研究对象+目标/方法”，不要照抄赛题，也不要堆叠算法名。

题目与逐问要求：
{问题}

模型建立、求解和验证证据：
{模型的建立与求解}
"""

toc = """
# 目录

只根据最终实际存在的章节生成目录，覆盖全部问题及其二级标题。
不要写正文，不要虚构页码，不要保留不存在的可选章节。

已完成的模型章节：
{模型的建立与求解}
"""

RepeatQues = """
# 一、问题重述
## 1.1 问题背景

用自己的语言压缩现实背景，只保留理解任务所必需的对象、场景、矛盾和价值。
不得大段照抄原题，不得虚构外部背景或文献。

## 1.2 已知条件与问题提出

先概括题设数据、常量、边界、规则、附件和提交格式，再按问题一、问题二……
分别说明输入、目标与要求输出。保留问题之间的依赖关系。

本章不得提前写完整公式推导、模型假设、代码结果或模型评价。

原始题目：
{题目}
"""

analysisQues = """
# 二、问题分析

为每一问建立独立二级标题，例如“## 2.1 问题一的分析”。
每一问必须回答：
1. 该问属于计算、预测、评价、优化、解释或仿真中的哪类任务；
2. 可用数据、题设规律和上一问结果是什么；
3. 关键难点、变量、目标和约束是什么；
4. 模型选择理由是什么，简单基线是否足够；
5. 预期输出是什么，采用什么验证；
6. 结果如何传入下一问。

问题分析承担路线图功能，不复制原题，不展开完整公式，不提前编造结果。
若提供总体逻辑，需解释各问的数据流和依赖。

题目：
{题目}

模型与求解摘要：
{模型的建立与求解}
"""

modelAssumption = """
# 三、模型假设

只写后文实际使用的题目特异假设。每条假设采用以下结构：
“假设名称：在何对象和范围内作何近似；题设、量级或数据依据是什么；
该假设简化了哪一数学关系；可能在哪些边界产生何种偏差。”

删除题设已经确认的事实和后文未使用的假设。
禁止写“数据真实可靠”“不考虑特殊情况”“市场完全稳定”等万能套话。

题目：
{题目}

模型与求解摘要：
{模型的建立与求解}
"""

symbol = """
# 四、符号说明和数据预处理
## 4.1 符号说明

使用Markdown表格，表格前写“**表1 符号说明**”。列至少包含：
| 符号 | 含义 | 单位 | 类型或取值域 |

优先列出跨章节使用的决策变量、状态变量、参数、集合、下标和评价指标。
相同符号全文保持一致；局部只出现一次的变量可在公式附近定义。
不得根据常见模型臆造未在正文出现的符号。

模型与求解摘要：
{模型的建立与求解}
"""

eda = """
## 4.2 数据来源与质量审计

先说明每个数据集的来源、时间/空间范围、原始样本量、关键字段和对应问题。
真实报告缺失、异常、重复、单位、编码、时间戳和跨表关联检查。

## 4.3 数据预处理与探索性分析

逐项说明处理公式或规则、阈值、采用理由和处理前后样本量，包括：
- 格式和单位统一；
- 缺失值与异常值处理；
- 时间/空间对齐和跨表合并；
- 特征构造及信息泄漏检查；
- 训练/验证/测试或时间滚动划分；
- 会影响模型选择的分布、趋势、周期或相关性。

只使用代码手真实输出的统计量和图表。每张可用EDA图都使用原始文件名，
alt text写“图X 中文图题”，并解释变量、趋势、异常及其对后续建模的影响。
不要堆叠与结论无关的描述性图表，不要编造样本量和统计量。
"""

ques1 = """
# 五、模型的建立与求解
## 5.1 问题一的模型建立与求解

按以下顺序形成完整闭环，可根据模型类型调整三级标题：

### 5.1.1 本问目标、变量与建模依据
直接说明本问需要交付的答案。定义变量、参数、集合、下标、单位和取值范围；
解释采用的机理、统计规律或业务规则及其适用条件。

### 5.1.2 模型建立
给出目标函数、约束、损失函数、机理方程或状态转移关系。
每个核心公式都解释现实含义、符号、量纲、来源和近似条件。
逐条将题目规则映射到数学关系，不用代码代替模型。

### 5.1.3 求解方法与参数
说明输入、初始化、算法步骤、关键参数、停止条件、随机种子、软件/求解器及
不可行或失败处理。参数必须来自题设、数据估计、调参或可核验文献。

### 5.1.4 结果、验证与本问结论
使用代码手的真实数值、表格和图片。解释数量级、极值、趋势、差异和现实意义，
并与题设阈值、简单基线或对照方案比较。选择匹配模型的验证，最后用一段
“本问结论”直接给出带单位、场景和合理精度的答案，并说明如何传入下一问。

只写问题一，不重复摘要、问题重述、全局分析和全局假设。
"""

ques2 = """
## 5.2 问题二的模型建立与求解

### 5.2.1 与问题一的衔接及本问目标
明确复用上一问的哪些结果，新增哪些数据、变量、目标或约束，以及本问需要交付的答案。

### 5.2.2 模型扩展或新模型
只引用公共模型并解释变化，避免复制上一问。定义新增符号，给出新增目标、约束、
方程或规则，解释现实含义、量纲和适用条件。

### 5.2.3 求解方法与参数
说明算法、初始化、关键参数、停止条件、随机种子、软件/求解器和选择理由。

### 5.2.4 结果、验证与本问结论
基于真实输出呈现表图和关键结果，进行基线/阈值比较和匹配的验证。
最后直接回答问题二，并说明结论是否稳定以及如何传入下一问。

只写问题二，不重复其他全局章节。
"""

ques3 = """
## 5.3 问题三的模型建立与求解

依次完成：
1. 与前序问题的衔接、本问目标和可交付答案；
2. 新增变量、参数、规律、目标、约束或状态转移关系；
3. 可复现的求解步骤、参数、停止条件、软件和随机设置；
4. 真实结果表图及数量级、趋势、差异和现实解释；
5. 匹配模型的验证、与基线或阈值的比较；
6. 带单位、场景和合理精度的“本问结论”。

只写问题三，不重复其他全局章节。
"""

ques4 = """
## 5.4 问题四的模型建立与求解

依次完成“前序衔接与目标—变量与模型—求解参数—真实结果—验证—本问结论”。
复用公共模型时只说明变化，不复制前文。所有数值、图表和评价指标必须来自真实输出；
证据缺失时明确标记，不得编造。

只写问题四，不重复其他全局章节。
"""

ques5 = """
## 5.5 问题五的模型建立与求解

依次完成“前序衔接与目标—变量与模型—求解参数—真实结果—验证—本问结论”。
突出相对前序问题新增的场景、约束、数据或决策。使用真实表图回答题目，
并说明结论的适用范围和稳定性。

只写问题五，不重复其他全局章节。
"""

ques6 = """
## 5.6 问题六的模型建立与求解

依次完成“前序衔接与目标—变量与模型—求解参数—真实结果—验证—本问结论”。
若本问是综合决策或推广，必须说明如何汇总前序输出、权衡不同目标并检验方案可行性。

只写问题六，不重复其他全局章节。
"""

sensitivity_analysis = """
# 六、模型的分析与检验
## 6.1 一致性与可行性检查

根据模型类型报告量纲、边界、守恒/平衡、硬约束、跨问题输入输出和基线复现检查。
给出判据、真实数值和是否通过，不得只写“模型有效”。

## 6.2 灵敏度分析

只选择会影响最终答案且存在不确定性的关键参数。报告基准值、扰动范围、步长或
情景、输出指标、最大变化、稳定区间和失稳阈值。结合真实图表说明现实含义。

## 6.3 不确定性或误差分析

说明测量、抽样、参数估计、离散近似、随机仿真或模型结构误差。
根据可用结果给出置信/预测区间、重复试验分布、误差传播或情景对比。

验证、敏感性和不确定性不得混为一谈；缺少运行证据时不得编造。
"""

judge = """
# 七、模型的评价、改进与推广
## 7.1 模型优点

每个优点都引用前文精度、约束满足、计算时间、可解释性、基线比较或稳健性证据。
禁止只写“精度高、适用范围广、操作简单”。

## 7.2 模型局限

说明具体失效条件、偏差方向、数据覆盖或计算限制，以及会影响哪项结论。

## 7.3 改进方向

逐一对应局限，说明需要新增的数据、模型模块、校准试验或计算资源，
以及预期改善的指标和成本。

## 7.4 模型推广

说明可以迁移到哪些对象、需要重新估计哪些参数、哪些假设不可直接迁移以及
最低数据条件。

本章不得重复完整求解过程，不得引入前文没有证据的新优势或新结果。
"""
