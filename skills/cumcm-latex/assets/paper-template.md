# [题目核心对象]的[建模目标或主要方法]

> 使用说明：复制后删除本说明和所有不适用的小节。将方括号占位符替换为真实证据；证据尚未产生时保留 `[待补：证据名称]`，禁止编造。章节编号可按实际问题数调整。

## 摘要

[用 1–2 句说明研究对象、题目中的核心矛盾、总体建模路线以及各问之间的关系。不要复述长背景。]

针对问题一，[说明任务类型和关键难点]。本文基于[题设规律/数据特征]，建立[模型全名]，以[核心变量]描述[对象]，采用[算法或求解器]求解。结果表明，[给出能直接回答问题的关键数值、区间、排序或方案，带单位]；通过[验证方式与指标]验证后，[给出误差、约束满足率或稳定性结论]。

针对问题二，[说明如何使用问题一的结果或新增信息]。本文建立[模型全名]，引入[新增变量/目标/约束]，利用[算法]得到[核心结果]。与[基线或对照方案]相比，[核心指标]由[基线值]变为[模型值]，[改善百分比或差值]为[数值]；在[扰动范围]内，结论[稳定性描述]。

针对问题三，[按“模型—求解—关键结果—验证”继续，不要只列算法名称]。

[若题目有更多问题，复制上一段。最后用 1 句概括模型的适用范围、总体可靠性或提交的支撑材料。]

**关键词：** [核心对象]；[主模型]；[关键算法]；[评价或验证方法]；[应用目标]

## 1 问题重述

### 1.1 问题背景

[用自己的语言压缩背景。说明对象、场景、现实矛盾和建模价值；只保留理解任务所必需的信息。]

### 1.2 已知条件

- [题设给定的数据、常量、边界或规则 1]
- [题设给定的数据、常量、边界或规则 2]
- [附件中可用的数据及时间、空间、单位范围]
- [题目要求遵守的输出格式或附件要求]

### 1.3 问题提出

1. **问题一：** [用一句话写清输入、目标和输出。]
2. **问题二：** [写清与问题一的依赖关系和新增要求。]
3. **问题三：** [写清任务。]
4. **问题四：** [若无则删除。]

## 2 问题分析与总体思路

### 2.1 问题一分析

[说明该问属于计算、预测、评价、优化、解释或仿真中的哪一类。指出关键难点、可利用的规律或数据、选择模型的理由、预期结果形式和验证方式。不要在此重复完整推导。]

### 2.2 问题二分析

[说明承接了上一问的哪些变量或结果，引入了哪些新目标和约束，为什么需要新的模型模块。]

### 2.3 其余问题分析

[按问题逐项写作。每项至少覆盖“目标—信息—难点—方法—输出—验证”。]

### 2.4 总体技术路线

![总体技术路线图](fig0_workflow.png)

**图 1  总体技术路线**

[解释图 1：原始数据如何经过预处理进入各模型，各问题之间传递什么结果，最终如何形成题目要求的答案。若没有有效流程图，删除图片占位符并用文字说明。]

## 3 模型假设

1. **[假设名称]：** 假设[具体对象在具体范围内满足什么条件]。理由是[题设依据、量级分析或数据证据]；该假设使得[被简化的模型关系]成立。
2. **[假设名称]：** 忽略[次要因素]，因为[其量级、持续时间或影响相对较小的依据]；由此可能在[边界情形]产生[偏差方向]。
3. **[假设名称]：** 在[适用范围]内，将[变量]视为[常量/平稳过程/独立变量]，其取值由[来源]确定。

[删除未在后文使用的假设。不要把“数据真实可靠”“不考虑特殊情况”作为无依据的万能假设。]

## 4 符号说明与数据处理

### 4.1 符号说明

| 符号 | 含义 | 单位 | 类型或取值域 |
|---|---|---:|---|
| $i\in\mathcal I$ | [对象索引] | — | 整数 |
| $t$ | [时间变量] | [s/h/d] | $[0,T]$ |
| $x_i$ | [决策或状态变量] | [单位] | [连续/整数/0-1] |
| $\theta$ | [待估参数] | [单位] | [范围] |
| $J$ | [目标函数或评价指标] | [单位] | [最小化/最大化] |

[全文符号较多时保留本表；仅出现一次的局部变量可在公式附近定义。]

### 4.2 数据来源与质量审计

| 数据集 | 来源 | 时间/空间范围 | 原始样本量 | 关键字段 | 用途 |
|---|---|---|---:|---|---|
| [附件 1] | [题目附件/公开来源] | [范围] | [数量] | [字段] | [对应问题] |

说明：

- 缺失情况：[字段、数量、比例和分布特征]。
- 异常情况：[判定规则、异常数量及是否可能是真实极端值]。
- 重复与一致性：[重复记录、单位、时间戳、编码和跨表关联检查]。
- 数据限制：[采样偏差、覆盖范围、测量精度或外部数据使用限制]。

### 4.3 数据预处理

1. **格式和单位统一：** [说明变换公式、时区/坐标/量纲处理。]
2. **缺失值处理：** [说明删除、插值或模型填补方法及理由；给出处理前后样本量。]
3. **异常值处理：** [说明识别方法、阈值和保留/修正规则。]
4. **特征构造：** [说明特征的数学定义、时间窗口和是否可能造成信息泄漏。]
5. **数据划分：** [训练/验证/测试或时间滚动划分；写出比例和随机种子。]

### 4.4 探索性分析

[只展示会影响模型选择的统计量和图形。解释趋势、相关性、周期性、分组差异或量级，不要堆叠无结论图表。]

| 指标 | 均值 | 标准差 | 最小值 | 中位数 | 最大值 |
|---|---:|---:|---:|---:|---:|
| [变量] | [值] | [值] | [值] | [值] | [值] |

## 5 问题一的模型建立与求解

### 5.1 本问目标与建模路线

[直接写本问要输出什么，以及为何采用当前模型。说明输入来自何处、输出供哪一问使用。]

### 5.2 变量、参数与模型机理

[定义本问新增变量和参数。若采用物理、几何、经济或统计规律，说明规律来源、适用范围和量纲。]

例如，若目标函数为

$$
\min_{\boldsymbol{x}} J(\boldsymbol{x})
=
\sum_{i\in\mathcal I} c_i x_i
+ \lambda P(\boldsymbol{x}),
\tag{1}
$$

其中，$c_i$ 表示[含义与单位]，$P(\boldsymbol{x})$ 表示[惩罚项含义]，$\lambda$ 控制[权衡关系]。式（1）对应题目的[具体目标]。

约束为

$$
\begin{aligned}
g_k(\boldsymbol{x}) &\le 0, && k=1,\ldots,m,\\
h_l(\boldsymbol{x}) &= 0, && l=1,\ldots,p,\\
\boldsymbol{x} &\in \mathcal X.
\end{aligned}
\tag{2}
$$

[逐条说明约束对应的现实规则、方向、单位和边界，不要只给公式。若不是优化问题，替换为机理方程、统计模型、评分规则或状态转移关系。]

### 5.3 求解方法与参数设置

[写清数据输入、初始化、迭代步骤、停止条件、随机种子、软件/求解器和关键超参数。解释选用该算法的理由，并给出时间复杂度或实际运行时间（若与结论有关）。]

**算法步骤：**

1. 输入[数据、参数和初始状态]。
2. 计算/更新[关键中间量]。
3. 检查[约束、收敛或停止条件]。
4. 输出[结果与对应文件]。

| 参数 | 含义 | 取值 | 取值依据 |
|---|---|---:|---|
| [参数] | [含义] | [数值] | [题设/估计/调参/文献] |

### 5.4 结果与解释

| 方案/对象 | 核心结果 | 单位 | 对比基线 | 变化率 | 是否满足约束 |
|---|---:|---:|---:|---:|---|
| [结果 1] | [数值] | [单位] | [数值] | [百分比] | [是/否] |

![问题一关键结果](fig1_result.png)

**图 2  [图中变量、对象和情景]**

[先引用图表，再解释至少三点：结果的数量级和方向；最重要的极值、拐点或分组差异；这些现象如何回答题目。不得只写“由图可知效果很好”。]

### 5.5 模型验证

[从下列方式中选择匹配本模型的验证，并给出指标和结果：量纲/边界检查、已知情形复现、约束满足、留出集误差、残差诊断、交叉验证、仿真重复、与基线或其他模型对比。]

| 验证项 | 指标或判据 | 结果 | 结论 |
|---|---|---:|---|
| [验证] | [阈值/指标] | [数值] | [是否通过及原因] |

### 5.6 本问结论

因此，问题一的直接答案为：[用完整句给出数值、区间、排序或方案，包含单位、场景和必要精度]。该结果将在问题二中作为[输入变量/边界/初始方案]。

## 6 问题二的模型建立与求解

### 6.1 与问题一的衔接及新增目标

[明确复用问题一的哪些结果，增加哪些变量、目标、约束或数据。]

### 6.2 模型扩展或新模型

[参照 5.2，写出新增数学关系。不要重复复制公共模型；只引用式（1）–（2）并解释变化。]

### 6.3 求解方法

[参照 5.3，写出算法与参数。若更换算法，解释原因；若沿用算法，说明调整。]

### 6.4 结果、验证与本问结论

[参照 5.4–5.6，用表图给出结果、验证，并以直接答案收束。]

## 7 问题三及后续问题的模型建立与求解

[复制“目标与衔接—模型—求解—结果—验证—本问结论”的闭环。按实际问题数拆成独立主章节。]

## 8 综合检验与敏感性分析

### 8.1 一致性与可行性检查

- 量纲检查：[关键公式左右量纲是否一致。]
- 边界检查：[极端输入时模型是否回到合理状态。]
- 约束检查：[硬约束满足率、最大违反量。]
- 跨问题检查：[前一问输出与后一问输入是否一致。]
- 守恒/平衡检查：[质量、能量、流量、数量或预算误差。]

### 8.2 敏感性分析

选择对结论最有影响且存在不确定性的参数，不要对所有参数机械扰动。

| 参数 | 基准值 | 扰动范围 | 输出指标 | 最大变化 | 结论是否改变 |
|---|---:|---:|---|---:|---|
| [参数] | [值] | [$-10\%\sim10\%$] | [指标] | [值] | [是/否] |

[解释敏感参数、稳定区间、失稳阈值及其现实含义。若结论改变，给出适用边界或备用方案。]

### 8.3 不确定性或误差分析

[说明误差来源：测量、抽样、参数估计、离散近似、随机仿真或模型结构。给出置信区间、预测区间、重复试验分布、误差传播或情景分析中的至少一种。]

## 9 模型评价、改进与推广

### 9.1 模型优点

1. **[可验证优点]：** [用前文指标或对比证据说明，而非空泛评价。]
2. **[结构优点]：** [例如变量解释清楚、约束可扩展、计算成本可接受。]

### 9.2 模型局限

1. **[局限]：** 当[条件]不成立时，会导致[偏差方向或失效表现]。
2. **[数据局限]：** 当前数据缺少[信息]，因此[某结论]只能用于[范围]。

### 9.3 改进方向

[逐一对应局限，说明新增数据、模型模块、校准实验或计算资源，以及预期改善的指标。]

### 9.4 模型推广

[指出可迁移到哪些对象；说明需要重新估计的参数、不能直接迁移的假设和最低数据条件。]

## 参考文献

[1] 作者. 题名[J]. 期刊, 年, 卷(期): 页码.

[2] 作者. 书名[M]. 出版地: 出版社, 年.

[3] 机构或作者. 页面/数据集名称[EB/OL]. 发布或更新日期[引用日期]. URL.

[4] 软件/数据集作者. 软件或数据集名称, 版本[CP/DB]. 年. DOI 或 URL.

> 仅保留正文实际引用且可核验的条目。统一格式；网页给出访问日期；数据和软件按需引用。

## 附录

### 附录 A 核心算法或代码说明

[只放复现结果所需的核心代码、伪代码或文件索引。写明运行环境、依赖版本、入口命令和随机种子。]

### 附录 B 补充推导与长表

[放置会打断正文但影响复核的完整推导、参数表或额外结果。]

### 附录 C 支撑材料清单

| 文件名 | 内容 | 对应问题 | 生成方式 |
|---|---|---|---|
| [文件] | [说明] | [问题] | [脚本/人工整理] |

[检查题目要求的结果文件名、格式和数量是否全部满足。]
